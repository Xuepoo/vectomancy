<!DOCTYPE html>
<html>
<head>
    <title>Vectomancy Desmos Export - {{ base_name }}</title>
    <script src="https://www.desmos.com/api/v1.9/calculator.js?apiKey=dcb31709b452b1cf9dc26972add0fda6"></script>
    <style>
        body, html { margin: 0; padding: 0; width: 100%; height: 100%; overflow: hidden; }
        #calculator { width: 100%; height: 100%; }
    </style>
</head>
<body>
    <div id="calculator"></div>
    <script>
        var elt = document.getElementById('calculator');
        var calculator = Desmos.GraphingCalculator(elt, {
            expressions: true,
            settingsMenu: true,
            zoomButtons: true
        });

        var expressions = [];
        var exprId = 1;

        {% if equations %}
        {% for path in equations %}
        {% for eq in path.data %}
        expressions.push({
            id: String(exprId++),
            latex: '\\left(' + 
                '{% for c in eq.x_poly %}{{ c }}{% if loop.index0 == 1 %}*t{% elif loop.index0 > 1 %}*t^{{ loop.index0 }}{% endif %}{% if not loop.last %} + {% endif %}{% endfor %}' +
                ', ' +
                '{% for c in eq.y_poly %}{{ c }}{% if loop.index0 == 1 %}*t{% elif loop.index0 > 1 %}*t^{{ loop.index0 }}{% endif %}{% if not loop.last %} + {% endif %}{% endfor %}' +
                '\\right)',
            parametricDomain: {min: '0', max: '1'},
            color: '{% if path.color_rgb %}rgb({{ path.color_rgb.0 }}, {{ path.color_rgb.1 }}, {{ path.color_rgb.2 }}){% else %}rgb(0,0,0){% endif %}'
        });
        {% endfor %}
        {% endfor %}
        {% elif strokes %}
        {% for stroke_path in strokes %}
        expressions.push({
            id: String(exprId++),
            latex: '\\left(' +
                '{% for term in stroke_path.data %}{% if not loop.first %} + {% endif %}{{ term.amplitude }}*\\cos({{ term.frequency }}*t + {{ term.phase }}){% endfor %}' +
                '{% if stroke_path.data | length == 0 %}0{% endif %}' +
                ', ' +
                '{% for term in stroke_path.data %}{% if not loop.first %} + {% endif %}{{ term.amplitude }}*\\sin({{ term.frequency }}*t + {{ term.phase }}){% endfor %}' +
                '{% if stroke_path.data | length == 0 %}0{% endif %}' +
                '\\right)',
            parametricDomain: {min: '0', max: '6.28318530718'},
            color: '{% if stroke_path.color_rgb %}rgb({{ stroke_path.color_rgb.0 }}, {{ stroke_path.color_rgb.1 }}, {{ stroke_path.color_rgb.2 }}){% else %}rgb(0,0,0){% endif %}'
        });
        {% endfor %}
        {% elif paths %}
        {% for path in paths %}
        expressions.push({
            id: String(exprId++),
            latex: '\\left[ {% for p in path.data %}\\left({{ p.x }}, {{ p.y }}\\right){% if not loop.last %},{% endif %}{% endfor %} \\right]',
            color: '{% if path.color_rgb %}rgb({{ path.color_rgb.0 }}, {{ path.color_rgb.1 }}, {{ path.color_rgb.2 }}){% else %}rgb(0,0,0){% endif %}',
            lines: true,
            points: false
        });
        {% endfor %}
        {% endif %}

        calculator.setExpressions(expressions);
    </script>
</body>
</html>